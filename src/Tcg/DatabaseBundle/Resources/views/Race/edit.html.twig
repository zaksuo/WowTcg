<h1>TcgRace edit</h1>

<form action="{{ path('race_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
    {{ form_widget(edit_form) }}
    <button type="submit">Modifier</button>
</form>

<a href="{{ path('race_list') }}">Retour</a>
<form action="{{ path('race_delete', { 'id': entity.id }) }}" method="post">
    {{ form_widget(delete_form) }}
    <button type="submit">Supprimer</button>
</form>
