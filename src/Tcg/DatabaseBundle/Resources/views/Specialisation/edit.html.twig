{% extends "TcgDatabaseBundle::layout.html.twig" %}

{% block content %}
    <form action="{{ path('specialisation_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
        <button type="submit">Sauvegarder</button> <a href="{{ path('specialisation_delete', { 'id': entity.id }) }}">Supprimer</a> <a href="{{ path('specialisation') }}">Retour</a>
    </form>
{% endblock %}