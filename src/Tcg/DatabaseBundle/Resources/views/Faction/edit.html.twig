{% extends "TcgDatabaseBundle::layout.html.twig" %}

{% block content %}
    <h1>TcgFaction edit</h1>

    <form action="{{ path('faction_update', { 'id': entity.id }) }}" method="post" {{ form_enctype(edit_form) }}>
        {{ form_widget(edit_form) }}
        <button type="submit">Modifier</button>

        <a href="{{ path('faction_delete', { 'id': entity.id }) }}">Supprimer</a>
        <a href="{{ path('faction_list') }}">Retour</a>
    </form>
{% endblock content %}
